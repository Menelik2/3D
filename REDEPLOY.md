# Redeploy trigger

Latest hero: 3D animated META PICTURES title (`MetaTitle3D`).

If the live site still shows the old logo box, Vercel is not building the latest `root` commit.

Set **Production Branch = root** and Redeploy without cache.
